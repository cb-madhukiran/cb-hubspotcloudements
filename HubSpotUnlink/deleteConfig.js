let formulaId = steps.loopOverFormulaConfigurations.entry.id;
let instanceId = steps.loopOverFormulaConfigurations.entry.instance;

let params = {
  formula: formulaId,
  instance: instanceId
}

done(params)